<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Телефонный справочник</h3>
            </div>
            <div class="panel-body">
                <?php if ($this->isAllowed('controller/Blog\Controller\Write:add', 'admin')) { ?>
            	    <a href="<?php echo $this->url('phone', array('action'=>'add'));?>">Add Contact</a>
		<?php }  ?>
                
                <?php if (isset($phones)):?>
                <table class="table">
                    <thead>
                    <tr>
                        <th>Id</th>
                        <th>Full name</th>
                        <th>email</th>
                        <th></th>
                    </tr>
                    </thead>
                
                <?php foreach($phones as $phone): ?>
                    <tbody>
                    <tr>
                        <td><?php echo $phone->getId(); ?></td>
                        <td><?php echo $phone->getFullName(); ?></td>
                        <td><?php echo $phone->getemail(); ?></td>
                        <td>
                            <a href="<?php echo $this->url('phone', 
                            array('action'=>'edit', 'id' => $phone->getId()));?>">Edit</a> 
                            <a href="<?php echo $this->url('phone', array('action'=>'delete', 'id' => $phone->getId()));?>">Delete</a>
                        </td>
                    </tr>
                    </tbody>
                <?php endforeach; ?>
                </table>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>